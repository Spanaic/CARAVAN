<h1>編集画面</h1>
<%= form_for(@blog) do |f| %>

    <h4>タイトル</h4>
    <%= f.text_field :title %>
    <h4>カテゴリ</h4>
    <%= f.text_field :category %>
    <h4>本文</h4>
    <%= f.text_area :body %>
    <%= f.submit "更新" %>
    <%# form_forヘルプメソッドは呼び出したら必ずendで閉じる %>
<% end %>